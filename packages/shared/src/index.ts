/**
 * @campusflow/shared — constants and contracts used by the CampusFlow apps.
 *
 * Source-only TypeScript package: Next.js transpiles it directly
 * (`transpilePackages` in apps/web/next.config.ts) so there is no build step and
 * no risk of the UI running against a stale compiled copy.
 */

export * from './domain';
export * from './contracts';
