# CampusFlow AI service — production image
# Build from apps/ai:  docker build -t campusflow-ai .
# Run:                 docker run -p 8000:8000 -e GEMINI_API_KEY=... campusflow-ai

# --- API / web framework -----------------------------------------------------
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.0
python-dotenv==1.0.1

# --- HTTP client (used to call Gemini's REST endpoint) -----------------------
httpx==0.28.1

# --- Vector search -----------------------------------------------------------
faiss-cpu==1.9.0.post1
numpy==2.2.1

# --- Testing -----------------------------------------------------------------
pytest==8.3.4
